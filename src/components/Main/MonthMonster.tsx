import { IMonthMonster } from "../../types/mainpageType";

export const MonthMonster = ({
  folioTitle,
  followCnt,
  nickname,
  profileImage,
  stacks,
}: IMonthMonster) => {
  return <li></li>;
};
