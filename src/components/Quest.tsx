import { IQuest } from "../types/mainpageType";

export const Quest = ({
  bookmarkCnt,
  classes,
  commentCnt,
  content,
  createdAt,
  duration,
  modifiedAt,
  nickname,
  questId,
  stacks,
  status,
  title,
}: IQuest) => {
  return <li></li>;
};
